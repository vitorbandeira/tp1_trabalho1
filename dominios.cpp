#include <iostream>
#include "dominios.h"
using namespace std;

bool ValidaNome(char string[20]){
	if(<=str[0]<=)//eh letra maiuscula
		erro = true;
	for (int i = 1; i < strlen(str); ++i)
		if(<=str[i]<=)//eh letra
			erro = true;
}
